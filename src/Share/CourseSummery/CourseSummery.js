import React from 'react';

const CourseSummery = ({ course }) => {
    const { picture, price, time, title, rating, details } = course;
    return (
        <div>

        </div>
    );
};

export default CourseSummery;